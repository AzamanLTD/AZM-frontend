// Smoke test placeholder. The original boilerplate `MyApp`/counter test
// from `flutter create` is no longer applicable to this app's bootstrapped
// Riverpod + GoRouter setup. Replaced with a minimal sanity check so
// `flutter analyze` and `flutter test` stay green until real widget tests
// are added under their own files.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder smoke test', () {
    expect(1 + 1, 2);
  });
}
